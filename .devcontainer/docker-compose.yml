services:
  dev:
    image: vscode-dev:node16
    container_name: vscode-dev
    build:
      context: .
      dockerfile: Dockerfile.dev
    user: 1000:1000
    volumes:
      - .:/workdir
    ports:
      - 3000:3000
      - 8080:8080
    network_mode: bridge
